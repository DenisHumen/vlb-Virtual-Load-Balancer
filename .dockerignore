# Build cache and dev artefacts that don't belong in the image.
target/
data/
*.db
*.db-*
.git/
.github/
.idea/
.vscode/
.claude/
provaider
todo.md
output.txt
systemd/
docker/
# ...except the test lab's entrypoint, which docker/test/vlb/Dockerfile has to
# COPY out of the repo-root build context.
!docker/test/vlb/entrypoint.sh
